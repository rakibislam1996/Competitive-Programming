#include <stdio.h>
void main()
{
    int N,X,i;
    while(scanf("%d",&N)!=EOF)
        {
            if(N==0)
                printf("vai ter copa!\n");
            else
                printf("vai ter duas!\n");
        }
}
